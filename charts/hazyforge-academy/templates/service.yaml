apiVersion: v1
kind: Service
metadata:
  name: {{ include "hazyforge-academy.fullname" . }}
  labels:
    {{- include "hazyforge-academy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hazyforge-academy.selectorLabels" . | nindent 4 }}
